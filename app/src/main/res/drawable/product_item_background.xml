<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền -->
    <solid android:color="@color/g_card_background" />
    <!-- Viền với độ dày và màu sắc -->
    <stroke
        android:width="1dp"
        android:color="@color/g_blue_gray200" />
    <corners android:radius="16dp" />
</shape>